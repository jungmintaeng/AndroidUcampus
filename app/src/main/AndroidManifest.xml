<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.dbhw1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Student_Main"
            android:label="학생 페이지">

        </activity>

        <activity android:name=".Register_Course"
            android:label="강의등록"/>

        <activity android:name=".Course_Plan"
            android:label="강의계획서"/>

        <activity android:name=".Instructor_Main"
            android:label="교수 페이지"/>
        <activity android:name=".Register"/>
        <activity android:name=".update_plan"/>
        <activity android:name=".Search"/>

    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>